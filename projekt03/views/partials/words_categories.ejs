<% categories.forEach(category=> { %>
    <section class="word-list-category">
        <h2>
            <%= category.name %>
        </h2>
        <ul>
            <% category.words.forEach(word => { %>
                <li>
                    <%= word.name %>

                        <a href="/word/edit/<%= word.id %>">
                            <button type="button">Edytuj</button>
                        </a>

                        <%- include("./forms/delete_word.ejs", { word }) %>
                </li>
                <% }) %>
        </ul>
    </section>
<% }) %>